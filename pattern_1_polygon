#the python code to create different polygonswith same base
import turtle
bob = turtle.Turtle()


def square(t,length,n):
     for i in range(n):
         t.fd(length)
         t.lt(360/n)
  
for n in range(3,22):
 square(bob,50,n)
